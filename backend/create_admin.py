import os, pymongo, bcrypt; from datetime import datetime, timezone; from dotenv import load_dotenv; load_dotenv(); db=pymongo.MongoClient(os.getenv('MONGO_URI'))['agropulse']; hashed_pw = bcrypt.hashpw('Admin123!'.encode('utf-8'), bcrypt.gensalt()).decode('utf-8'); res = db.users.update_one({'email': 'admin@agropulse.com'}, {'$set': {'password': hashed_pw, 'displayName': 'System Admin', 'role': 'admin', 'isApproved': True, 'isActive': True, 'createdAt': datetime.now(timezone.utc), 'updatedAt': datetime.now(timezone.utc)}}, upsert=True); print('Admin user created.')
